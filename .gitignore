# Runtime data
/data/
*.db
*.db-shm
*.db-wal

# Go build output
/dockmesh
/dockmesh.exe
/dockmesh-agent
/dockmesh-agent.exe
/dmctl
/dmctl.exe
/bin/
/dist/
/cmd/dockmesh/web_dist/*
!/cmd/dockmesh/web_dist/.gitkeep

# Frontend build
/web/build/
/web/.svelte-kit/
/web/node_modules/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude Code local (per-user, not shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks/

# Test / coverage
*.out
coverage.txt

# Playwright MCP session artifacts (screenshots, snapshots)
.playwright-mcp/

# Root-level design iteration screenshots (logos, dashboard mockups)
# Kept out of git; archived separately if needed.
/logo-*.png
/p5-*.png

# Local marketing assets — pushed to dockmesh.dev, not the app repo
/marketing/screenshots/

# Temporary working files
/tmp_*.md
/tmp_*.json
