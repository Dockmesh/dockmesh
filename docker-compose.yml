services:
  dockmesh:
    build:
      context: .
      args:
        VERSION: dev
    image: dockmesh:dev
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data:/app/data
      - ./stacks:/app/stacks
    restart: unless-stopped
